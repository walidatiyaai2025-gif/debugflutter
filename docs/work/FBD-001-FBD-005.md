# FBD-001 / FBD-005 — Solution Foundation & Domain Contracts

Status: `REVIEW`

Issue: #2

Branch: `agent/fbd-001-005-foundation-closeout`

Base: `agent/fbd-foundation` after PR #5 merge (`dc3fb145a9a4351e6ab319e45e76e9bcec10befd`)

## Objective

Close the remaining acceptance gap for the solution foundation and shared domain contracts without redesigning the existing architecture.

## Verified foundation state

- `FlutterBuildDoctor.sln` exists and includes the current source/test project graph.
- Unit and integration test projects exist.
- Windows GitHub Actions restore/build/test validation exists.
- Shared diagnostic statuses include Ready, Running, Missing, Outdated, Incompatible, Broken, Warning, Error, Cancelled, RepairAvailable, and Fixed.
- Domain contracts are isolated from WPF and concrete infrastructure dependencies.
- Process contracts support asynchronous/cancellable execution and are consumed by infrastructure.
- PR #5 removed compile blockers found when the full solution was first validated.

## Remaining gap found during closeout

Issue #2 requires an initial root README explaining the build/run entry point. The foundation branch did not contain `README.md`.

## Implementation

- Added root `README.md` with:
  - product/foundation scope
  - prerequisites
  - solution/project map
  - deterministic restore/build/test commands
  - WPF run command
  - architecture rules
  - links to implementation plan, task board, and per-task work notes

## Validation gates

The closeout PR must pass the existing Windows CI gates:

- `dotnet restore FlutterBuildDoctor.sln`
- `dotnet build FlutterBuildDoctor.sln --configuration Release --no-restore`
- `dotnet test FlutterBuildDoctor.sln --configuration Release --no-build`

## Completion rule

Move Issue #2 to `DONE` only after this closeout PR is green and merged into `agent/fbd-foundation`. After that, FBD-002 and FBD-003 become the first unblocked P0 foundation tasks.