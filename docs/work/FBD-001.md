# FBD-001 — Create .NET 8 solution and project structure

Status: DONE
Priority: P0 Critical
Owner role: Tech Lead
Branch: `agent/fbd-001-foundation`

## Scope implemented

- Added `FlutterBuildDoctor.sln` targeting Visual Studio 2022 / .NET 8.
- Added the eight planned production projects under `src/`:
  - `FlutterBuildDoctor.App`
  - `FlutterBuildDoctor.Application`
  - `FlutterBuildDoctor.Domain`
  - `FlutterBuildDoctor.Infrastructure`
  - `FlutterBuildDoctor.Flutter`
  - `FlutterBuildDoctor.Android`
  - `FlutterBuildDoctor.Repair`
  - `FlutterBuildDoctor.Persistence`
- Added the two planned test-project shells under `tests/`:
  - `FlutterBuildDoctor.UnitTests`
  - `FlutterBuildDoctor.IntegrationTests`
- Added a minimal WPF bootstrap (`App.xaml` / `MainWindow.xaml`) only to make the application project buildable. Navigation/MVVM/DI remain intentionally out of scope for FBD-002/FBD-101.
- Added `global.json` for .NET 8 SDK selection and a baseline `.gitignore`.
- Established project references according to the planned layer direction without introducing third-party dependencies.

## Validation receipt

Temporary GitHub Actions run: `31296925056` (`FBD-001 Validation`).

Validation executed on `windows-latest` with .NET 8:

1. Solution project count = 10 — PASS.
2. `dotnet restore FlutterBuildDoctor.sln` — PASS.
3. `dotnet build FlutterBuildDoctor.sln --configuration Release --no-restore` — PASS.

The temporary validation workflow was removed after the successful run so permanent CI remains owned by FBD-008.

## Acceptance criteria

`Solution builds with all planned projects and tests` — VERIFIED.

## Next critical-path task

`FBD-002 — Add CommunityToolkit.Mvvm + Generic Host + DI`.
