# FBD-502 — Parse Flutter Doctor sections

Status: DONE pending final-head CI and merge

## Scope

Parse recognized sections from the raw `ProcessResult` produced by FBD-501. FBD-502 does not execute Flutter, mutate the environment, repair anything, or normalize raw output. Unknown-output modeling remains FBD-503.

## Implementation

- Added typed parser contracts:
  - `FlutterDoctorParseStatus`
  - `FlutterDoctorSectionKind`
  - `FlutterDoctorSectionStatus`
  - `FlutterDoctorSection`
  - `FlutterDoctorParseResult`
  - `IFlutterDoctorParser`
- Added `FlutterDoctorParser`.
- Recognized section kinds include Flutter, Windows Version, Android toolchain, Chrome, Visual Studio, Android Studio, VS Code, Connected device, Network resources, Xcode, and Linux toolchain.
- Maps doctor header markers to typed states:
  - `✓` and Windows-style `√` → Ready
  - `!` → Warning
  - `✗`, `X`, and `x` → Error
  - `-` → NotApplicable
  - any other marker → Unknown
- Leading whitespace before a doctor header is accepted without changing the preserved raw header line.
- Structured section parsing is restricted to stdout. stderr remains available in the full process evidence but cannot become or contaminate a typed section.
- Every recognized section retains its exact source stdout `ProcessOutputLine` records.
- The parse result retains the complete original `ProcessResult`, including exit status/code, command, failure/receipt evidence, stdout, and stderr. `SourceOutput` remains a convenience view over the original output collection.
- Any syntactically valid but unrecognized doctor header terminates the current recognized section, so unknown details cannot leak into the preceding recognized section.
- Parsing still runs when FBD-501 returned a failed process result if usable doctor evidence exists.
- Registered `IFlutterDoctorParser` in production runtime-detection DI.

## Coverage

Unit coverage verifies:
- representative official-style Flutter/Android/Android Studio/device/network sections
- Ready/Warning/Error/NotApplicable/Unknown marker mapping, including `√`, `X`, and `x`
- leading-whitespace headers
- recognized section source-line preservation
- complete `ProcessResult` and source-output preservation
- stderr header isolation
- unknown header boundaries
- parsing useful evidence from a failed execution
- explicit no-process-evidence state
- explicit no-recognized-sections state without dropping raw evidence

Integration coverage verifies singleton DI composition.

## Validation

- Earlier feature validation: workflow `31319489987`, job `93260053656`, on head `fadae29cfa5322aa7abc6a3f4633783bd7c0fe04` passed Restore, Release Build, full tests, artifact upload, and cleanup.
- Robustness update validation: workflow `31319664192`, job `93260480832`, on head `23e613d3fe9205c0c7ca03b82dbe62d3be96b68c` passed Restore, Release Build, the full Unit/Integration test suite, test-result artifact upload, and cleanup.

## Boundaries

- FBD-503 owns explicit modeling/preservation behavior for unknown doctor output.
- FBD-504 owns the structured `flutter --version` probe.
- FBD-505 owns the Doctor detail UI.
- FBD-506 owns the larger doctor parser fixture matrix.
- No repair behavior is introduced.

## Next critical-path task

FBD-601 — Locate Flutter project root.

FBD-503 and FBD-504 are available as parallel Flutter Doctor follow-ups, but M1 continues through the Project Analyzer path after FBD-502.
