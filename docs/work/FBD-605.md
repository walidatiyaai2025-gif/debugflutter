# FBD-605 — Parse Gradle wrapper version

Status: DONE pending final PR-head CI and merge

## Goal

Read the Gradle wrapper configuration under the effective FBD-601 Flutter project root and expose the configured Gradle distribution version without executing Gradle or mutating the repository.

## Implementation

- Uses one canonical `IGradleWrapperVersionParser` / `GradleWrapperVersionParser` with typed result statuses and distribution kind (`bin` / `all`).
- Reads only `android/gradle/wrapper/gradle-wrapper.properties`.
- Enforces a 256 KiB file-size limit and 2048 logical-line parsing limit.
- Supports Java-properties separators, escapes, Unicode escapes, and line continuations.
- Parses the standard `gradle-<version>-bin.zip` / `gradle-<version>-all.zip` archive shape, including prerelease versions.
- Rejects conflicting duplicate `distributionUrl` values rather than silently selecting one.
- Rejects project/Android/Gradle/wrapper/properties reparse-point boundaries.
- Returns explicit missing/invalid/read/unsafe/version-not-found states.
- Registers the parser as a singleton in production DI.

## Integration correction

Concurrent work initially produced two FBD-605 parser contracts/implementations. They were unified into the single `GradleWrapperVersionParser` contract. The duplicate `GradleWrapperParser` implementation, contract, tests, and DI registration were removed. The retained implementation includes the teammate work for duplicate-URL ambiguity and bounded logical-line parsing plus the added reparse/DI coverage.

## Security boundary

The result contract intentionally does **not** expose raw `gradle-wrapper.properties` text. A custom `distributionUrl` can contain embedded credentials or query tokens, so only sanitized URL evidence is returned: user-info, query string, and fragment are removed. Error messages do not echo the raw URL. Security regression coverage verifies this contract.

## Safety boundary

FBD-605 is read-only configuration parsing. It does not run `gradlew`, Gradle, Flutter, Dart, Java, or network requests; it does not download distributions or modify wrapper files. AGP and Kotlin plugin parsing remain FBD-606/FBD-607.

## Coverage

Coverage includes:

- standard bin/all URLs and prerelease versions
- Java-properties continuation/escape handling
- sanitized credential/query/fragment evidence
- missing wrapper properties and missing `distributionUrl`
- conflicting duplicate `distributionUrl` values
- nonstandard archive names
- invalid Unicode escape handling without echoing secret input
- 256 KiB size limit
- Android directory and properties-file reparse/symlink rejection
- no-raw-properties result contract
- production DI singleton composition

## Validation

- PR validation run `31324441240` / job `93272437761` on head `eaaf5d6cc6b12c901a5c11b118e037c1094f71e4` passed Restore, Release Build, full Tests, artifact upload, and cleanup.

## Next

FBD-606 — Parse Android Gradle Plugin version after FBD-605 is merged and the newest integration checkpoint is re-read.
